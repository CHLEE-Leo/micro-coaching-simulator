# AGENTS.md — 메타 레벨 운영 원칙

이 파일은 특정 작업 지시문이 아니라, 작업 전반에 일관되게 적용할 메타 레벨 운영 원칙을 모아두는 기준 문서다.

## 기본 원칙

1. 현재 작업 방향이 이 원칙과 충돌하지 않는지 확인한다.
2. 이 파일에는 특정 기능, 특정 버그, 특정 파일 하나를 위한 지침을 쓰지 않는다.
3. 반복적으로 유용하다고 확인된 작업 습관만 일반화된 원칙으로 추가한다.
4. 새로운 지침을 추가할 때는 앞으로도 여러 작업에 재사용될 수 있는지 먼저 점검한다.

## 작업 진행 원칙

1. 구현, 리팩터링, 문서화는 가능한 한 함께 정리하여 코드와 설명이 어긋나지 않게 한다.
2. 명령이나 수정이 끝난 뒤에는 관련 코드 흐름을 다시 검토해 불필요한 코드, 어색한 인터페이스, 문서 드리프트가 없는지 확인한다.
3. 작업이 끝나면 변경된 파일뿐 아니라 연관 파일의 주석·docstring·아키텍처 다이어그램도 전수 검토하여 현재 구현과 일치하도록 갱신한다.
4. 선언만 되어 있고 실제 동작에 연결되지 않은 설정이나 옵션은 그대로 두지 말고, 연결하거나 제거한다.
5. 임시 산출물, 테스트 찌꺼기, 캐시 파일처럼 저장소에 남길 가치가 낮은 항목은 정리한다.

## 문서화 원칙

1. README와 사용 예시는 현재 실제 동작하는 실행 경로를 기준으로 유지한다.
2. 문서는 미래 계획보다 현재 구현 상태와 검증된 사용법을 우선해서 설명한다.
3. 문서가 길어질수록 개요, 실행 방법, 검증 상태, 다음 단계처럼 빠르게 스캔 가능한 구조를 유지한다.

## 설계 원칙

1. 구조는 한 번의 작업을 빨리 끝내기 위한 임시 결합보다, 이후 확장과 교체가 쉬운 모듈 경계를 우선한다.
2. 설정, 서비스 조립, 코어 로직, 입출력 스키마는 가능한 한 분리한다.
3. 새 기능을 넣을 때는 기존 코드와 문서의 이름, 책임, 데이터 흐름이 일관되게 유지되는지 함께 본다.

## 품질 원칙

1. 수정 후에는 최소한 문법 검증이나 실행 가능한 예제를 통해 현재 경로가 깨지지 않았는지 확인한다.
2. 검증하지 못한 부분은 문서나 보고에서 분명하게 밝힌다.
3. 작은 어색함이라도 반복될 가능성이 있으면 바로잡고, 일회성이라면 지침으로 승격하지 않는다.

## AGENTS.md 갱신 원칙

1. 이 파일은 작업을 수행하면서 더 나은 메타 습관이 분명해질 때만 갱신한다.
2. 새 항목을 추가할 때는 기존 원칙과 중복되지 않도록 합치거나 치환한다.
3. 이 파일이 장황한 체크리스트가 되지 않도록, 실제로 방향 유지에 도움이 되는 원칙만 남긴다.
