fastapi>=0.110.0
uvicorn[standard]>=0.29.0
jinja2>=3.1.3
python-multipart>=0.0.9
pydantic>=2.0.0

# LLM 추론 — vLLM 대신 llama-cpp-python 사용 (CPU/GPU 모두 지원)
# pip install llama-cpp-python         ← CPU only
# pip install llama-cpp-python[cuda]   ← NVIDIA GPU (CUDA)
# pip install llama-cpp-python[metal]  ← Apple Silicon GPU
llama-cpp-python>=0.2.70

# ChatGPT API (LangGraph + LangChain)
langchain-openai>=0.3.0
langgraph>=0.2.0
python-dotenv>=1.0.0
